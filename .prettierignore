# Ignore files for PNPM, NPM and YARN
pnpm-lock.yaml
package-lock.json
yarn.lock

# Local dfx state — never committed.
.dfx/

# icp-cli state: `data/` holds the canister ID mapping (committed, but not
# ours to format), `cache/` is generated.
.icp/

# JSON5 with comments that document the header rules — left as written.
static/.ic-assets.json5
